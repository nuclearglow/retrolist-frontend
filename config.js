// This is client side config only - don't put anything in here that shouldn't be public!
export const endpoint = 'http://10.10.10.100:3000/api/graphql';
export const prodEndpoint = `fill me in when we deploy`;
